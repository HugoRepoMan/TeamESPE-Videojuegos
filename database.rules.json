{
  "rules": {
    "liveOverlay": {
      ".read": true,
      ".write": "auth !== null && (auth.token.admin === true || auth.token.email === 'admin@teamespe.com')"
    },
    "bracketsLive": {
      "$disciplineId": {
        ".read": true,
        ".write": "auth !== null && (auth.token.admin === true || auth.token.email === 'admin@teamespe.com')"
      }
    },
    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
